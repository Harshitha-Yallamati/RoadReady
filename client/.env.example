VITE_API_BASE_URL=https://roadready-3vcx.onrender.com/api
VITE_API_PROXY_TARGET=http://localhost:5000

# Firebase config is currently hardcoded in src/firebase.js.
# For multiple environments, these can be moved to env vars prefixed with VITE_.
